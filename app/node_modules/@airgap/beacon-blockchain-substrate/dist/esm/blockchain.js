import { extensionList, desktopList, webList, iOSList } from './ui/alert/wallet-lists';
export class SubstrateBlockchain {
    identifier = 'substrate';
    async validateRequest(input) {
        // TODO: Validation
        if (input) {
            return;
        }
    }
    async handleResponse(input) {
        // TODO: Validation
        if (input) {
            return;
        }
    }
    async getWalletLists() {
        return {
            extensionList: extensionList,
            desktopList: desktopList,
            webList: webList,
            iOSList: iOSList
        };
    }
    async getAccountInfosFromPermissionResponse(permissionResponse) {
        return permissionResponse.blockchainData.accounts.map((account) => ({
            accountId: account.accountId,
            address: account.address,
            publicKey: account.publicKey
        }));
    }
}
//# sourceMappingURL=blockchain.js.map