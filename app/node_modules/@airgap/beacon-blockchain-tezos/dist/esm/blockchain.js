import { desktopList, extensionList, iOSList, webList } from './ui/alert/wallet-lists';
export class TezosBlockchain {
    identifier = 'xtz';
    async validateRequest(input) {
        // TODO: Validation
        if (input) {
            return;
        }
    }
    async handleResponse(input) {
        // TODO: Validation
        if (input) {
            return;
        }
    }
    async getWalletLists() {
        return {
            extensionList: extensionList,
            desktopList: desktopList,
            webList: webList,
            iOSList: iOSList
        };
    }
    async getAccountInfosFromPermissionResponse(_permissionResponse) {
        return [{ accountId: '', address: '', publicKey: '' }];
    }
}
//# sourceMappingURL=blockchain.js.map